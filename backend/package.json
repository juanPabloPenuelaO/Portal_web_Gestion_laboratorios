{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "db:migrate": "echo \"Omitido: DB_USE_EXISTING=true — usar tablas de Supabase\" && exit 1",
    "db:migrate:undo": "echo \"Omitido: DB_USE_EXISTING=true\" && exit 1",
    "db:seed": "echo \"Omitido: DB_USE_EXISTING=true — datos ya en Supabase\" && exit 1",
    "db:seed:jpenuela": "sequelize-cli db:seed --seed 20260327000005-usuario-jpenuela.js",
    "db:reset": "echo \"Omitido: DB_USE_EXISTING=true\" && exit 1",
    "db:inspect": "node scripts/inspect-schema.js",
    "db:expected": "node scripts/expected-schema.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "sequelize-cli": "^6.6.5"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.4",
    "pg": "^8.21.0",
    "sequelize": "^6.37.8"
  }
}
